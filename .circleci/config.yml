version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout

     # build the application image
    #  - run: docker build -t helloworld-gke .

     # deploy the image
    #  - run: docker push helloworld-gke

      - run: |
          TAG=0.1.$CIRCLE_BUILD_NUM
          docker build -t CircleCI-Public/circleci-demo-docker:$TAG .
          echo f06d4bff30be5318288c | docker login -u punktapl --password-stdin
          docker push CircleCI-Public/circleci-demo-docker:$TAG