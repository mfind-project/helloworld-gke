version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     # start proprietary DB using private Docker image
     # with credentials stored in the UI
     - run: |
         echo f06d4bff30be5318288c | docker login --username punktapl --password-stdin
         docker run -d --name db company/proprietary-db:1.2.3

     # build the application image
     - run: docker build -t company/helloworld-gke .

     # deploy the image
     - run: docker push company/helloworld-gke